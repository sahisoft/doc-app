import { RenderBylineDirective } from './render-byline.directive';

describe('RenderBylineDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderBylineDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
