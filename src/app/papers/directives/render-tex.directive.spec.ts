import { RenderTexDirective } from './render-tex.directive';

describe('RenderTexDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderTexDirective(null);
    expect(directive).toBeTruthy();
  });
});
